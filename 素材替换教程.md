# 桌面宠物 - 素材替换教程

## 快速替换（无需重新编译）

> 注意：当前版本的宠物图片已嵌入到 EXE 内部，因此替换素材需要重新发布。
> 如果你想实现"直接替换文件即可换肤"，请参考下方【进阶：外部素材模式】。

### 步骤

1. **准备新素材**
   - 格式：PNG（支持透明通道）
   - 建议尺寸：256x256 像素或更大（程序会自动缩放）
   - 背景：完全透明（Alpha 通道），避免带纯色背景
   - 推荐工具：Photoshop、GIMP、在线去背景工具（remove.bg）
   - 需要准备三张图片：
     - `正面.png` - 静止时显示
     - `奔跑1.png` - 奔跑动画帧1
     - `奔跑2.png` - 奔跑动画帧2

2. **替换图片文件**
   - 将三张图片分别命名为 `正面.png`、`奔跑1.png`、`奔跑2.png`
   - 覆盖项目目录中的 `DesktopPet\Resources\img\` 下的对应文件

3. **重新发布**
   - 先确保桌面宠物已关闭（右键菜单 → 退出，或任务管理器结束 `DesktopPet` 进程）
   - 在 PowerShell 中执行（以下路径为示例，请根据实际项目位置调整）：
   ```powershell
   # 示例：使用相对路径
   cd DesktopPet
   dotnet publish DesktopPet.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -o ..\publish
   
   # 或使用绝对路径（根据你的实际路径）
   dotnet publish 你的项目路径\DesktopPet.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -o 你的输出路径\publish
   ```
   - 如果提示文件被占用，说明宠物还没关掉，先执行以下命令结束进程：
   ```powershell
   taskkill /F /IM DesktopPet.exe
   ```
   然后再重新发布

4. **更换 EXE 图标（可选）**
   - 准备图标文件：将 `正面.png` 或其他图片转换为 `.ico` 格式
     - 在线转换工具：https://convertico.com/ 或 https://icoconvert.com/
     - 建议包含多种尺寸：16x16、32x32、48x48、256x256
   - 将图标文件命名为 `icon.ico`，放到 `DesktopPet\Resources\` 目录
   - 打开 `DesktopPet.csproj`，确保包含以下配置：
     ```xml
     <ApplicationIcon>Resources\icon.ico</ApplicationIcon>
     ```
   - 重新发布，新生成的 EXE 就会使用自定义图标

5. **运行**
   - 双击发布目录下的 `DesktopPet.exe`（如 `publish\DesktopPet.exe`）

---

## 进阶：外部素材模式（推荐）

如果你希望**不重新编译就能换角色**，可以修改代码让程序从 EXE 同目录加载图片。

### 修改方法

打开 `PetForm.cs`，找到 `LoadPetImage()` 方法，将逻辑改为优先读取外部文件：

```csharp
private static Image LoadPetImage()
{
    // 优先从 EXE 同目录的 Resources 文件夹加载
    var exeDir = AppDomain.CurrentDomain.BaseDirectory;
    var localPath = Path.Combine(exeDir, "Resources", "pet.png");
    if (File.Exists(localPath))
        return Image.FromFile(localPath);

    // 回退到嵌入资源
    var assembly = typeof(PetForm).Assembly;
    var resourceName = assembly.GetManifestResourceNames()
        .FirstOrDefault(n => n.EndsWith("pet.png", StringComparison.OrdinalIgnoreCase));
    if (resourceName != null)
    {
        using var stream = assembly.GetManifestResourceStream(resourceName);
        if (stream != null)
            return Image.FromStream(stream);
    }

    throw new FileNotFoundException("未找到宠物图片 pet.png");
}
```

修改后重新发布，然后在 `publish` 目录下创建 `Resources\img` 文件夹，放入三张图片：

```
D:\app\桌宠\publish\
├── DesktopPet.exe
└── Resources\
    └── img\
        ├── 正面.png    ← 静止时显示
        ├── 奔跑1.png   ← 奔跑动画帧1
        └── 奔跑2.png   ← 奔跑动画帧2
```

之后只需替换这三张图片文件，无需重新编译。

---

## 修改 EXE 图标

EXE 文件的图标需要在编译时嵌入，步骤如下：

1. **准备图标文件**
   - 使用 `正面.png` 或其他图片
   - 转换为 `.ico` 格式（建议包含 16x16、32x32、48x48、256x256 多种尺寸）
   - 在线转换工具：https://convertico.com/ 或 https://icoconvert.com/

2. **放置图标文件**
   - 将生成的图标命名为 `icon.ico`
   - 放到 `DesktopPet\Resources\icon.ico`

3. **修改项目配置**
   - 打开 `DesktopPet.csproj`
   - 确保包含以下配置：
   ```xml
   <ApplicationIcon>Resources\icon.ico</ApplicationIcon>
   ```

4. **重新发布**
   - 执行发布命令，新生成的 EXE 就会使用自定义图标

---

## 分发给他人

**是的，可以直接将 EXE 发给别人使用！**

### 文件结构

```
D:\app\桌宠\publish\
├── DesktopPet.exe          ← 主程序（单文件，约150MB）
└── Resources\
    └── img\
        ├── 正面.png        ← 素材文件（必须）
        ├── 奔跑1.png       ← 素材文件（必须）
        └── 奔跑2.png       ← 素材文件（必须）
```

### 分发方式

**方式1：整个 publish 文件夹打包（推荐）**
- 将 `publish` 文件夹压缩成 ZIP
- 发送给别人
- 别人解压后双击 `DesktopPet.exe` 即可运行
- **优点**：可以自定义素材，别人也能替换图片换肤

**方式2：只发送 EXE（不推荐）**
- 只发送 `DesktopPet.exe` 文件
- **缺点**：缺少素材文件，程序会报错或显示默认图片
- 除非你将素材嵌入到 EXE 内部（需要修改代码）

### 路径说明

✅ **代码中使用的是相对路径**：
```csharp
var exeDir = AppDomain.CurrentDomain.BaseDirectory;
var imgDir = Path.Combine(exeDir, "Resources", "img");
```

这意味着：
- 无论 EXE 放在哪个目录，程序都会在同级目录查找 `Resources\img\`
- 可以放在桌面、D盘、U盘等任何位置
- 别人收到后，只要保持 `DesktopPet.exe` 和 `Resources` 文件夹的相对位置不变，就能正常运行

### 注意事项

1. **不要单独移动 EXE**：如果把 EXE 单独复制到别处，而 `Resources` 文件夹留在原地，程序会找不到图片
2. **保持目录结构**：`DesktopPet.exe` 和 `Resources` 文件夹必须在同一目录下
3. **系统要求**：Windows 10/11，不需要安装 .NET 运行时（已打包在 EXE 中）

---

## 自定义短句

打开 `PhraseManager.cs`，修改 `Phrases` 数组即可：

```csharp
private static readonly string[] Phrases =
[
    "你的短句1",
    "你的短句2",
    "你的短句3",
    // ... 添加任意多条
];
```

---

## 调整默认参数

打开 `PetSettings.cs` 可修改：

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `Scale` | 初始缩放倍率 | 0.25 |
| `MinScale` / `MaxScale` | 缩放范围 | 0.1 ~ 2.0 |
| `OriginalSize` | 基础尺寸(px) | 64 |
| `IdlePeriod` | 闲置弹跳周期(ms) | 1200 |
| `IdleAmplitude` | 闲置弹跳幅度(px) | 5 |

速度等级在 `SpeedMap` 中调整（像素/帧）：
- 慢：1.0
- 正常：3.0
- 快：6.0

---

## 常见问题

**Q: 宠物有粉色/白色背景方块？**
A: 素材 PNG 的透明区域不是真正的透明（Alpha=0），而是填充了纯色。请用图片编辑软件清除背景，确保透明区域 Alpha=0。

**Q: 宠物移动有残影？**
A: 程序使用 `UpdateLayeredWindow` + 每帧清空画布，正常情况下不会有残影。如果出现，可能是显卡驱动问题，尝试更新驱动。

**Q: 宠物太大/太小？**
A: 右键菜单可以实时缩放（30%~200%），也可以修改 `PetSettings.cs` 中的 `OriginalSize` 调整基础尺寸。

**Q: 如何开机自启？**
A: 将 `DesktopPet.exe` 的快捷方式放到 `shell:startup` 目录（`Win+R` 输入 `shell:startup` 回车）。
